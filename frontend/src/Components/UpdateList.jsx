import React, { useState } from 'react'

function UpdateList() {
    const[eiting,setEditing]=useState(false);
    const[title,setTitle]= useState("")
  return (
    <div>UpdateList</div>
  )
}

export default UpdateList